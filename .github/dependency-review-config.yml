# Pull-request dependency gate.  Keep this allow-list synchronized with the
# reviewed dependency inventory in docs/dependency-policy.md and sbom.cdx.json.
fail-on-severity: high
fail-on-scopes:
  - runtime
  - development
allow-licenses:
  - MIT
  - BSD-3-Clause
